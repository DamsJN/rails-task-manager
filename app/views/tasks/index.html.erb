<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <h4><%= link_to task.title, task_path(task) %></h4>
  <h4><%= task.details %></h4>
  <h4><%= task.completed %></h4>

  <%= link_to "Delete",
      task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>
